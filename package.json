{
  "name": "captcha-screenshot-generator",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p .",
    "gen": "npm run build && node dist/generator.js --config config.json"
  },
  "dependencies": {
    "playwright": "^1.48.2",
    "zod": "^3.23.8",
    "picocolors": "^1.0.0",
    "cli-progress": "^3.12.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}